export interface AnswerValue {
  data: string;
  isCorrect: boolean;
}

export interface QuestionValue {
  title: string;
  answers: AnswerValue[];
}

const Question = () =>{
<></>

}

export default Question;
